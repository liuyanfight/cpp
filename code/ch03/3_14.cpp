#include <iostream>
#include <vector>
using namespace std;

int main()
{
  vector<int> v1;
  int i;
  while(cin >> i) {
    v1.push_back(i);
  }
  // string s;
  // while(cin >> s) {
  //   v1.push_back(s);
  // }
  return 0;
}